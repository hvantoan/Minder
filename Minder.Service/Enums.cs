namespace Minder.Service {

    public class Enums {

        public enum EUpdateType {
            Stadium = 1,
            Time = 2,
        }

        public enum EAutoMation {
            Location = 1,
            Time = 2
        }

        public enum ETeamSide {
            Host = 1,
            Opposite = 2,
        }
    }
}